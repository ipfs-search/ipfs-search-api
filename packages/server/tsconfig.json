{
  "extends": "../../tsconfig.json",

  "compilerOptions": {
    "rootDir": "src",
    "outDir": "dist",

    // https://www.npmjs.com/package/ts-node#missing-types
    "typeRoots": [
      "./typings",
      "./node_modules/@types",
      "../../node_modules/@types"
    ]
  }
}
